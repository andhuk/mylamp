# Lampa Plugin Development Kit

> Атомарні компоненти, шаблони та документація для швидкої розробки плагінів Lampa

## � Про проєкт

Цей проєкт надає повний набір інструментів для розробки плагінів для [Lampa](https://lampa.mx) - веб-додатку для перегляду фільмів та серіалів.

**Що включено:**
- 🧩 Атомарні компоненти (menu, buttons, settings, storage, API)
- 📝 Шаблони плагінів (basic, menu, settings)
- 🎬 Робочі приклади (random movie, watch history)
- 📚 Повна документація Lampa API
- 🎨 Колекція SVG іконок
- ⚡ VS Code сніпети

## 🎬 Готові плагіни

### 1. Clean Menu - Управління меню налаштувань

Приховування та управління порядком розділів в меню налаштувань (Налаштування, Синхронізація, Інтерфейс, Канали, Плеєр, etc.).

**Встановлення в Lampa:**
```
https://andhuk.github.io/mylamp/examples/clean-menu.js
```

**Функції:**
- Приховування/показ розділів налаштувань через чекбокси
- Зміна порядку розділів стрілками вгору/вниз
- Модальне вікно редактора з візуальним інтерфейсом
- Збереження при натисканні "Назад"
- Скидання до початкового стану

### 2. Історія переглядів (Watch History)

Відстежує та зберігає історію переглянутих фільмів зі статистикою.

**Встановлення в Lampa:**
```
https://andhuk.github.io/mylamp/examples/watch-history.js
```

**Функції:**
- Автоматичне відстеження переглядів
- Статистика (всього, унікальних, останнє оновлення)
- Список останніх 5 фільмів
- Очищення історії
- Розділ в налаштуваннях

### 3. Lampa UI - Netflix-style інтерфейс

Трансформує інтерфейс Lampa в стиль Netflix з темною темою, закругленими кутами, акцентними червоними елементами та покращеною типографікою.

**Встановлення в Lampa:**
```
https://andhuk.github.io/mylamp/examples/lampa-ui.js
```

**Функції:**
- Темна тема (#141414) з червоними акцентами (#e50914)
- Закруглені кути карток (8-12px)
- Ефект фокусу з білою рамкою та збільшенням
- Netflix типографіка (Netflix Sans)
- Мінімалістична бічна панель (60px)
- Градієнтні накладки для читабельності тексту
- Кнопки CTA з градієнтним стилем
- Горизонтальні каруселі з анімаціями
- Налаштування для увімкнення/вимкнення

### 4. Better UI — Netflix-style інтерфейс

Плагін перетворює Lampa UI на стиль Netflix: Hero-банер з градієнтами, типографіка, інформаційні теги, CTA (Play / More info), горизонтальні каруселі, мінімалістична бічна панель, верхнє меню, картки з закругленнями та focus state (біла рамка + масштаб), темна тема з фірмовим червоним.

**Встановлення в Lampa:**
```
https://andhuk.github.io/mylamp/examples/better-ui.js
```

**Функції:**
- Hero Section: великоформатний контент з темним градієнтом під текстом
- Типографіка: чітка ієрархія заголовків та описів
- Інформаційні теги: рейтинг, сезони, рік, жанри
- CTA: контрастні кнопки Play та More info
- Горизонтальні каруселі та категоризація контенту
- Sidebar 60px з мінімалістичними іконками
- Top Navigation та система карток (закруглені кути, focus — біла рамка + scale)
- Темна тема (#141414) та акцентний червоний (#e50914)
- Налаштування для увімкнення/вимкнення

---

## 🚀 Як встановити плагін в Lampa

1. Відкрийте **Lampa**
2. Перейдіть в **Налаштування** → **Розширення** (або **Плагіни**)
3. Натисніть **Додати плагін**
4. Вставте URL плагіна (з розділу вище)
5. Натисніть **Додати**
6. Перезавантажте Lampa

## 📁 Структура проєкту

```
mylamp/
├── README.md                 # Цей файл
├── LAMPA_API.md             # Повна документація API
├── agent-guidelines.md      # AI workflow гайди
├── icons.md                 # SVG іконки
│
├── components/              # Атомарні компоненти
│   ├── menu-item.js        # Пункт меню
│   ├── head-button.js      # Кнопка в шапці
│   ├── settings-section.js # Розділ налаштувань
│   ├── storage-manager.js  # Менеджер Storage
│   └── api-client.js       # API клієнт
│
├── templates/              # Шаблони плагінів
│   ├── basic-plugin.js    # Базовий плагін
│   ├── menu-plugin.js     # Плагін з меню
│   └── settings-plugin.js # Плагін з налаштуваннями
│
├── examples/              # Робочі приклади
│   ├── clean-menu.js     # 🎬 Плагін: управління меню
│   ├── random-movie.js   # 📝 Приклад: випадковий фільм
│   ├── watch-history.js  # 🎬 Плагін: історія переглядів
│   ├── lampa-ui.js       # 🎬 Плагін: Netflix-style UI
│   └── better-ui.js      # 🎬 Плагін: Netflix-style (Hero, CTA, каруселі)
│
└── .vscode/              # VS Code інтеграція
    └── lampa-snippets.code-snippets
```

## 🛠️ Для розробників

### Швидкий старт (30 секунд)

```javascript
(function () {
    'use strict';
    
    function startPlugin() {
        Lampa.Noty.show('Привіт від мого плагіна!');
    }
    
    if (window.appready) startPlugin();
    else Lampa.Listener.follow('app', (e) => {
        if (e.type === 'ready') startPlugin();
    });
})();
```

### Використання шаблонів

Виберіть шаблон з `templates/`:
- **basic-plugin.js** - мінімальний плагін
- **menu-plugin.js** - плагін з пунктом меню
- **settings-plugin.js** - плагін з налаштуваннями

### Використання компонентів

```javascript
// Пункт меню
createMenuItem({
    id: 'my_item',
    icon: '<svg>...</svg>',
    text: 'Мій пункт',
    onClick: () => Lampa.Noty.show('Клік!')
});

// API запит
var api = createApiClient();
api.tmdb('movie/popular', {page: 1}, (data) => {
    console.log('Movies:', data.results);
});
```

## 📖 Документація

- **[LAMPA_API.md](./LAMPA_API.md)** - повна документація Lampa API
- **[agent-guidelines.md](./agent-guidelines.md)** - AI workflow гайди
- **[icons.md](./icons.md)** - колекція SVG іконок (50+)

## 🧩 Компоненти

Готові атомарні компоненти в папці `components/`:

- **menu-item.js** - пункт меню навігації
- **head-button.js** - кнопка в шапці
- **settings-section.js** - розділ налаштувань
- **storage-manager.js** - менеджер Storage
- **api-client.js** - API клієнт

## 🔧 VS Code

Сніпети вже включені! Введіть `lampa-` та виберіть потрібний:
- `lampa-basic` - базовий плагін
- `lampa-menu` - пункт меню
- `lampa-settings` - налаштування
- `lampa-api` - API запит
- та інші...

## 🔗 Посилання

- **GitHub**: https://github.com/andhuk/mylamp
- **Lampa**: https://lampa.mx
- **Lampa Source**: https://github.com/yumata/lampa-source
- **Приклади плагінів**: https://github.com/and7ey/lampa

## 📄 Ліцензія

MIT License - вільне використання для будь-яких цілей

---

## 📋 Історія змін / Fix log

### 2025-02-15 — clean-menu.js: "Розділи не знайдено. Відкрийте налаштування спочатку."

**Проблема:** Після натискання «Управління розділами» в налаштуваннях Clean Menu з’являлося повідомлення «Розділи не знайдено. Відкрийте налаштування спочатку.»

**Знайдення:**
- Плагін викликав `Lampa.Activity.back()` і через 500 ms читав DOM `.settings .settings-param[data-component]`.
- Після повернення екран головного списку налаштувань міг ще не встигнути відмалюватися, тому селектор повертав 0 елементів.
- Фіксована затримка 500 ms була ненадійною (різні пристрої/швидкість анімації).

**Зміни:**
1. **Поллінг після back():** Замість однієї перевірки через 500 ms — опитування DOM кожні 200 ms до 25 спроб (~5 s). Як тільки знайдено розділи — відкривається редактор.
2. **Спроба без back():** Спочатку перевіряється поточний DOM; якщо список розділів уже є на екрані — редактор відкривається без виклику `back()`.
3. **getSettingsItems():** Додано запасний селектор `.settings-list .settings-param[data-component]` і fallback для назви (якщо `.settings-param__name` порожній — береться перший рядок тексту елемента).

**Файли:** `examples/clean-menu.js`

---

**Версія:** 1.0.0 | **Автор:** andhuk
